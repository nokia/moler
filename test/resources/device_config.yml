LOGGER:
  PATH: /tmp/
  RAW_LOG: True
  DATA_FORMAT: '%d %H:%M:%S'

DEVICES:
  CREATE_AT_STARTUP: True
  
  DEFAULT_CONNECTION:
    CONNECTION_DESC:
      io_type: terminal
      variant: threaded

  UNIX:
    DEVICE_CLASS: moler.device.unixremote.UnixLocal
