language: python
python:
  - 2.7
  - 3.6
install:
  - pip install -U pip
  - pip install -U coveralls
before_script:
  - pip install -rU requirements.txt
  - python2 -V
  - python3 -V
script:
  - coverage run --source=moler,test -m py.test -vv
after_success:
  - coveralls
  - env
  - uptime
